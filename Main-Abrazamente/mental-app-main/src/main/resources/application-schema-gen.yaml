spring:
  datasource:
    url: jdbc:h2:mem:schema_gen_db
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      jakarta:
        persistence:
          schema-generation:
            scripts:
              action: create
              create-target: target/schema.sql
